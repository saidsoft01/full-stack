/*
      const hour = 6;
      const name = 'Simon';
      let night = `Good Nignt`;

      if (hour >= 6 && hour <= 12) {
        console.log(`Good morning ${name}!`);
      } else if (hour >= 13 && hour <= 17) {
        console.log(`Good afternoon ${name}!`);
      } else if (hour >= 13 && hour <=24) {
       console.log(night);
      }else {
        console.log('you should be asleep...go to bed.');
      }

      const age = 6;
      const isHoliday = !true;

      if ((age <= 6 || age >= 65) && isHoliday){
        console.log('Discounted Price');
      }
      else {
        console.log(`Not Discounted price`);
      }



      const randNum = Math.random();
      let computer = '';

      if (randNum <= 0.5) {
        computer = 'heads';
      }
      else {
        computer = 'tails';
      }

      const  guess = 'head';
      
      if (computer === guess) {
        console.log('you win');
      }
      else {
        console.log('you lose');
      }
      */

      cartQuantity = 0;















